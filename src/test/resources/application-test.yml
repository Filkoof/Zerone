spring:
  mail:
    host: smtp.mail.ru
    username: zerone.mail@mail.ru
    password: hcaPst9hn8USn45TkqLW
    port: 465
    protocol: smtps
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.yaml
    drop-first: true
  test:
    database:
      replace: none
  datasource:
    url: jdbc:h2:mem:soc_net;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS soc_net
    username: sa
    password:
server:
  servlet:
    session:
      tracking-modes: cookie
      persistent: false
  port: ${PORT:8080}

config:
  frontend: "195.161.114.18:80"
  backend: "195.161.114.18:8086"
  token-validity-hours: 72
  authorization: "AUTHORIZATION"
  secret: SocialNetwork26
  zeroneEmail: zerone.mail@mail.ru