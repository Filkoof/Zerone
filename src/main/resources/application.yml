
spring:
  datasource:
    url: jdbc:${DATABASE_URL:postgresql://localhost:5432/postgres?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}

  mail:
    host: smtp.mail.ru
    username: zerone.mail@mail.ru
    password: hcaPst9hn8USn45TkqLW
    port: 465
    protocol: smtps
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
  web:
    resources:
      static-locations: [classpath:/templates/]

server:
  servlet:
    session:
      tracking-modes: cookie
      persistent: false
  port: ${PORT:8080}


mail:
  hostBack: "195.161.114.18:8086"
  hostFront: "195.161.114.18:80"
    #hostBack: "localhost:8086"
    #hostFront: "localhost:80"


config:
  domain: "195.161.114.18:8086"
  token-validity-hours: 72
  authorization: "AUTHORIZATION"
  secret: SocialNetwork26









